train:
  _target_: data_module.dataset.AudioSuperResolutionDataset
  content: /oss-speechllm-a100/xuxuenan/audio_super_resolution/moises/train/content.jsonl
  audio: /oss-speechllm-a100/xuxuenan/audio_super_resolution/moises/train/audio.jsonl
  base_content_path: /oss-speechllm-a100/xuxuenan/audio_super_resolution/moises
  base_audio_path: /oss-speechllm-a100/xuxuenan/audio_super_resolution/moises
  target_sr: ${sample_rate}
  downsampling_ratio: ${downsampling_ratio}
  use_h5_cache: false
  task_instruction: ${instruction_embedding}
  max_duration: 5.0
  random_crop: true
val:
  _target_: data_module.dataset.AudioSuperResolutionDataset
  content: /oss-speechllm-a100/xuxuenan/audio_super_resolution/moises/val/content.jsonl
  audio: /oss-speechllm-a100/xuxuenan/audio_super_resolution/moises/val/audio.jsonl
  base_content_path: /oss-speechllm-a100/xuxuenan/audio_super_resolution/moises
  base_audio_path: /oss-speechllm-a100/xuxuenan/audio_super_resolution/moises
  target_sr: ${sample_rate}
  downsampling_ratio: ${downsampling_ratio}
  use_h5_cache: false
  task_instruction: ${instruction_embedding}
  max_duration: 5.0
  random_crop: false
  max_samples: ${max_val_samples}
test:
  _target_: data_module.dataset.AudioSuperResolutionDataset
  content: /oss-speechllm-a100/xuxuenan/audio_super_resolution/moises/val/content.jsonl
  base_content_path: /oss-speechllm-a100/xuxuenan/audio_super_resolution/moises
  target_sr: ${sample_rate}
  downsampling_ratio: ${downsampling_ratio}
  use_h5_cache: false
  task_instruction: ${instruction_embedding}
  instruction_idx: 1
  random_crop: false
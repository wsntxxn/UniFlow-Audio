train:
  _target_: data_module.dataset.AudioSuperResolutionDataset
  content: /oss-speechllm-a100/xuxuenan/audio_super_resolution/musdb/train/content.jsonl  # frequency-cutoff audio
  audio: /oss-speechllm-a100/xuxuenan/audio_super_resolution/musdb/train/audio.jsonl  # high-frequency enhanced audio
  base_content_path: /oss-speechllm-a100/xuxuenan/audio_super_resolution/musdb
  base_audio_path: /oss-speechllm-a100/xuxuenan/audio_super_resolution/musdb
  target_sr: ${sample_rate}
  downsampling_ratio: ${downsampling_ratio}
  use_h5_cache: false
  task_instruction: ${instruction_embedding}
  max_duration: 5.0
  random_crop: true
val:
  _target_: data_module.dataset.AudioSuperResolutionDataset
  content: /oss-speechllm-a100/xuxuenan/audio_super_resolution/musdb/val/content.jsonl  # frequency-cutoff audio
  audio: /oss-speechllm-a100/xuxuenan/audio_super_resolution/musdb/val/audio.jsonl  # high-frequency enhanced audio
  base_content_path: /oss-speechllm-a100/xuxuenan/audio_super_resolution/musdb
  base_audio_path: /oss-speechllm-a100/xuxuenan/audio_super_resolution/musdb
  target_sr: ${sample_rate}
  downsampling_ratio: ${downsampling_ratio}
  use_h5_cache: false
  task_instruction: ${instruction_embedding}
  max_duration: 5.0
  random_crop: false
  max_samples: ${max_val_samples}
test:
  _target_: data_module.dataset.AudioSuperResolutionDataset
  content: /oss-speechllm-a100/xuxuenan/audio_super_resolution/musdb/test/content.jsonl
  base_content_path: /oss-speechllm-a100/xuxuenan/audio_super_resolution/musdb
  target_sr: ${sample_rate}
  downsampling_ratio: ${downsampling_ratio}
  use_h5_cache: false
  task_instruction: ${instruction_embedding}
  instruction_idx: 1
  random_crop: false
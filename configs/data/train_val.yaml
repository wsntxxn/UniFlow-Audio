defaults:
  - default
  - _self_

train_data_list:
  # tts
  - ${..libritts.train}
  # tta
  - ${..audiocaps.train}
  # svs
  - ${..m4singer.train}
  # sr
  - ${..ttshq.train}
  - ${..musdb.train}
  - ${..moises.train}
  # se
  - ${..ljspeech+musan.train}
  - ${..libritts_100+wham.train}
  - ${..libritts_360+wham.train}
  - ${..voicebank+demand.train}
  - ${..vctk+wham.train}
  # v2a
  - ${..vggsound_clip.train}
  # ttm
  - ${..msd.train}

val_data_list:
  # tts
  - ${..libritts.val}
  # tta
  - ${..audiocaps.val}
  # svs
  - ${..m4singer.val}
  # sr
  - ${..ttshq.val}
  - ${..musdb.val}
  - ${..moises.val}
  # se
  - ${..ljspeech+musan.val}
  - ${..libritts_100+wham.val}
  - ${..libritts_360+wham.val}
  - ${..voicebank+demand.val}
  - ${..vctk+wham.val}
  # v2a
  - ${..vggsound_clip.val}
  # ttm
  - ${..msd.val}
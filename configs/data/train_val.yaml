defaults:
  - default
  - _self_

train_data_list:
  # tts
  - ${..libritts.train}
  # tta
  - ${..audiocaps.train}
  # svs
  - ${..m4singer.train}
  # sr
  - ${..ttshq.train}
  - ${..musdb.train}
  - ${..moises.train}
  # se
  - ${..ljspeech_musan.train}
  - ${..libritts_100_wham.train}
  - ${..libritts_360_wham.train}
  - ${..voicebank_demand.train}
  - ${..vctk_wham.train}
  # v2a
  - ${..vggsound_clip.train}
  # ttm
  - ${..msd.train}

val_data_list:
  # tts
  - ${..libritts.val}
  # tta
  - ${..audiocaps.val}
  # svs
  - ${..m4singer.val}
  # sr
  - ${..ttshq.val}
  - ${..musdb.val}
  - ${..moises.val}
  # se
  - ${..ljspeech_musan.val}
  - ${..libritts_100_wham.val}
  - ${..libritts_360_wham.val}
  - ${..voicebank_demand.val}
  - ${..vctk_wham.val}
  # v2a
  - ${..vggsound_clip.val}
  # ttm
  - ${..msd.val}
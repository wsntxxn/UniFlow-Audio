defaults:
  - default
  - _self_

test_data_list:
  # tts
  - ${..libritts.test}
  # tta
  - ${..audiocaps.test}
  # svs
  - ${..m4singer.test}
  # sr
  - ${..esc_audiosr.test}
  - ${..vctk_audiosr.test}
  - ${..musdb.test}
  # se
  - ${..voicebank_demand.test}
  # ttm
  - ${..music_caps.test}
  # v2a
  - ${..vggsound_clip.test}
defaults:
  - default
  - _self_

train_data_list:
  - ${..ttshq.train}
  - ${..musdb.train}
  - ${..moises.train}

val_data_list:
  - ${..ttshq.val}
  - ${..musdb.val}
  - ${..moises.val}

test_data_list:
  - ${..esc_audiosr.test}
  - ${..vctk_audiosr.test}
  - ${..musdb.test}